version: '3.8'

services:
  tvbox-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tvbox-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
